$(window).load(function() {
  $("#loader").fadeOut(4200);
})


$(document).ready( function() {
	
	// Loading Gif & FadeIn all sections  
    
	


    // Scrolling Speed
    
    $('.navbar').smint({
    	'scrollSpeed' : 1000
    });

    

    // Reveal Scroll Top Button when scrolling down

 //    var startY= $('#bio').position().top + $('#bio').outerHeight();
	// $('.back_top').html($('#nav').html());
	// $(window).scroll(function () {
	//     if($(this).scrollTop() > startY ){
	//         $('.back_top').slideDown();
	//     }else{
	//         $('.back_top').slideUp();
	//     }
	// });
});

	





  


